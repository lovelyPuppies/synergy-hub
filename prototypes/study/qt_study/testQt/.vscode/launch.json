{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run testQt",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build-host/testQt", // 빌드된 실행 파일 경로
      "args": [],
      "cwd": "${workspaceFolder}/build-host", // 실행 디렉터리
      "stopAtEntry": false,
      "externalConsole": false, // 외부 콘솔 사용 여부
      "MIMode": "lldb", // 또는 "gdb", 시스템에 맞게 변경
    }
  ]
}